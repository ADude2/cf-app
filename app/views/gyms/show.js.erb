$('.reviews').html("<%= escape_javascript(render partial: 'reviews/review', collection: @reviews) %> ");
$('.paginator').html("<%= escape_javascript(will_paginate @reviews, renderer: BootstrapPagination::Rails) %>");
$('.pagination a').attr('data-remote', 'true');
<% @reviews.each do |review| %>
  $(".coaching_<%= review.id %>").raty({
    starOn: "<%= image_path('star-on.png') %>",
    starHalf: "<%= image_path('star-half.png') %>",
    starOff: "<%= image_path('star-off.png') %>",
    score: <%= review.coaching %>,
    readOnly: true
  });

  $(".equipment_<%= review.id %>").raty({
    starOn: "<%= image_path('star-on.png') %>",
    starHalf: "<%= image_path('star-half.png') %>",
    starOff: "<%= image_path('star-off.png') %>",
    score: <%= review.equipment %>,
    readOnly: true
  });

  $(".pricing_<%= review.id %>").raty({
    starOn: "<%= image_path('star-on.png') %>",
    starHalf: "<%= image_path('star-half.png') %>",
    starOff: "<%= image_path('star-off.png') %>",
    score: <%= review.pricing %>,
    readOnly: true
  });    

  $(".location_<%= review.id %>").raty({
    starOn: "<%= image_path('star-on.png') %>",
    starHalf: "<%= image_path('star-half.png') %>",
    starOff: "<%= image_path('star-off.png') %>",
    score: <%= review.location %>,
    readOnly: true
  });

  $(".score_<%= review.id %>").raty({
    starOn: "<%= image_path('star-on.png') %>",
    starHalf: "<%= image_path('star-half.png') %>",
    starOff: "<%= image_path('star-off.png') %>",
    score: <%= review.score %>,
    readOnly: true
  });
<% end %>
